extends layout
block content
    h1 All Modules
    table(border=1)
        for item in data
            tr
                td #{item.code}
                td
                    a(href='/module-single/' + item.code) #{item.name}