extends layout
block content
    h1 Programme: #{programme.name}
    p Programme ID: #{programme.id}
    h2 Modules
    table(border=1)
        tr
            th Code
            th Name
        for item in programme.modules
            tr
                td #{item.code}
                td #{item.name}