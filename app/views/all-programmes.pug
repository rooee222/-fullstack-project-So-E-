extends layout
block content
    h1 All Programmes
    table(border=1)
        for item in data
            tr
                td #{item.id}
                td
                    a(href='/programme-single/' + item.id) #{item.name}